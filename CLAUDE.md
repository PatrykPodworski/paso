# paso

Tracker: GitHub Issues (`gh`, repo PatrykPodworski/paso). Team work follows the `team-workflow` skill — parent issue + sub-issues, `blocked-by` relations, status labels `in-progress` / `in-review`.
